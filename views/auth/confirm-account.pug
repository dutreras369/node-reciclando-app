extends ../layout/index

block contents  
    div.py-10
        h1.text-center.text-4xl.my-10.font-extrabold Reci
            span.font-normal clando
        h2.text-center.text-2xl.font-extrabold= page
    div(class="bg-white shadow py-8 px-4 rounded-lg max-w-md mx-auto")
        p( class= error ? 'bg-red-400' : 'bg-green-500').py-2.px-5.rounded-lg.w-full.font-bold.text-center.text-white.uppercase= msg

        if !error
            a(href="/auth/login" class="text-center font-bold text sm text-slate-600 uppercase mt-10 block") ya puedes iniciar sesion